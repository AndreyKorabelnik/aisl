# Current continuation pointer — 2026-08-19

Continue with **GitHub bootstrap only**.

Order:

1. create GitHub repository;
2. import this complete bootstrap tree;
3. inspect `git status` and repository structure;
4. commit and push;
5. record `git rev-parse HEAD` as Source canonical;
6. use `release-YYYY.MM.DD.N` for future release tags;
7. only then resume functional development.

Do not start the parked operational observability block automatically.
