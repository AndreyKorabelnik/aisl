# aisl-reporting 0.8.0

- Added versioned `PackageRequest` and `PackageRunManifest` contracts.
- Added `sdd-draft-package/v1` with deterministic `data-evidence.yaml`.
- Added bounded one-call SDD/OpenSpec synthesis and conservative projection.
- Added reserved-path, evidence-ID, schema, budget and owner-confirmation guardrails.
- Added CLI commands: `package-prepare`, `package-build`, `package-project`.
- Added UCP regression fixture and migration expectations for the old OpenSpec profile.
