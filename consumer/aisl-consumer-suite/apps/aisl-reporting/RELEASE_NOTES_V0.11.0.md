# aisl-reporting 0.11.0

## Iteration 26

- embeds canonical `analysis_coverage/v1` in the system-description, data-model and workspace-interaction datasets;
- requires renderers to distinguish observed facts, interpretation and limitations;
- prevents categorical absence claims when coverage is partial;
- states that coverage counts are diagnostic occurrences, not accuracy percentages.
