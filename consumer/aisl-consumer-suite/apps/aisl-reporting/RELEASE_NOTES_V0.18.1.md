# AISL Reporting 0.18.1

- Reads derived product database identity through canonical `physical_artifacts[]` role `database`.
- Removes the remaining assumption that published KnowledgeProducts expose a top-level `database` object.
- Reporting semantics and report generation are unchanged.
