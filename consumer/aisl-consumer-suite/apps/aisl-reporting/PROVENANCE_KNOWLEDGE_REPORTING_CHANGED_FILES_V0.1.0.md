# Changed files — aisl-reporting 0.1.0

Initial release. Includes report contracts, profile loading, deterministic dataset preparation, renderer adapters, validation, regression comparison, CLI and `system-description/v1`.
