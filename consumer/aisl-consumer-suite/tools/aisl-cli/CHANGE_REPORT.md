# aisl-cli 0.2.0 — Change Report

- Consumer dependency changed from the former `aisl-client` distribution to `aisl-sdk==0.3.0`.
- Imports now use `aisl_sdk`.
- CLI commands and Knowledge API semantics are unchanged.
- CLI remains a thin shell over the public SDK.
