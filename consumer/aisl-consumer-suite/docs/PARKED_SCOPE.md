# Parked scope

The former Build-shell parked item is resolved by `aisl-workbench 0.4.0`: the UI is implemented directly against the public KCP contract instead of reconstructing the historical Generator UI.

Still parked / separate:
- security hardening beyond existing extension points;
- public package registries and compatibility machinery;
- provider-specific remote LLM network validation.
