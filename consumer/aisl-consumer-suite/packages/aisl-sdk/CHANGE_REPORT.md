# aisl-sdk 0.3.0 — Change Report

- Clean-break rename of Python distribution `aisl-client` to `aisl-sdk`.
- Python import package renamed from `aisl_client` to `aisl_sdk`.
- Public class/API semantics are unchanged; `AislClient` remains the primary client object.
- No compatibility package, alias, re-export or legacy dependency was added.
- Existing compact data-model projection and Integration Profile behavior are unchanged.
