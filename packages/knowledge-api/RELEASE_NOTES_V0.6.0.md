# knowledge-api 0.6.0

## Iteration 26

- publishes revision-aware `GET /api/knowledge/v1/systems/{system_id}/coverage`;
- exposes the canonical `analysis_coverage/v1` contract without recomputing or interpreting KLC facts;
- preserves count basis, domain statuses, grouped limitations and the `absence of evidence is not absence` statement;
- expands the canonical OpenAPI document to 13 paths.
