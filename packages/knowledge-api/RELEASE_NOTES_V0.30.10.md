# knowledge-api 0.30.10

- Publishes the Prepared Runtime 0.1.0.post7 universal exact-read expansion for `persistence-lineage/v1` through the existing knowledge-item endpoint.
- Aligns canonical package dependencies with `knowledge-integration==0.1.8` and `prepared-knowledge-runtime==0.1.0.post7`.
- No new Knowledge API execution or materialization path is introduced; the API remains a thin read boundary over the published typed product.
