# Changed files — 0.38.0

- Add deterministic object-centric data-model context projection and public GET endpoint.
- Enrich declared objects with exact published logical/model-storage semantics when those products are members of the selected revision.
- Preserve explicit `not_available`/`not_observed` states instead of inferring missing storage or physical SQL joins.
- Wire the new read tool into the Data Model Consumer Kit profile.
- Relax the Knowledge Integration dependency to the supported 0.1.x contract line with minimum 0.1.16.
