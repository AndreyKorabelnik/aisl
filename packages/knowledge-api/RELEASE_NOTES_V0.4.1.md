# knowledge-api 0.4.1

## Iteration 23

- accepts canonical collection lineage evidence (`storage_lineage_id`, `composed_expression`);
- exposes collection membership interpretation status in the v3 relationship contract;
- preserves all polymorphic aliases without selecting one target;
- regenerates executable OpenAPI from the current contract.

The top-level data-model schema remains `data_model_api/v3`; the added fields are optional members of its generic evidence model.
