# Knowledge API 0.30.4

Adds a thin public export boundary for the reusable `llm_integration_profile/v1` owned by `knowledge-integration`, including an HTTP endpoint and deterministic `consumer-kit` CLI export. No knowledge production or new Knowledge Layer logic is introduced.
