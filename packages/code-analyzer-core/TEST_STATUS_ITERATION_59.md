# Test status — iteration 59

## Focused and adjacent automated tests

- 119 passed, 0 failed.
- Added canonical artifact tests for:
  - exact versioned shard contract;
  - JSONL structure and record counts;
  - SHA-256 and byte-size verification;
  - absence of legacy fact types;
  - absence of machine-local paths;
  - top-level manifest and run-result references;
  - deterministic shards, coverage, and content fingerprint;
  - partial coverage for ambiguous SQL without guessing;
  - streaming validation of a valid artifact;
  - detection of a tampered shard without raising an unhandled exception.
- Existing SQL script, placeholder, scoped relation/column, target binding, JOIN, direct/recursive lineage, mart, schema, source-scope, table-observation, system-description, conceptual-model, observation-contract, universal-source-observation, and version tests remain green.

## Static and contract checks

- `compileall`: passed.
- Runtime/package version consistency: passed.
- Contract: `sql-analysis/v1` / `1.0`.
- Canonical fact types: 17.
- Absolute-path scan: passed.
- Repeated generic policy-field scan: passed.
- Full-artifact streaming validation: passed.
- Partial coverage is reported as warning, not failure.

## Real repository smoke and determinism regression

`datamart_profile_fl`:

- files scanned: 480;
- SQL units: 306;
- SQL statements: 475;
- canonical records: 27,600;
- artifact size: 46.2 MiB;
- full diagnostic output: 753.8 MiB;
- size ratio: approximately 6.1%;
- analysis status: partial;
- content fingerprint:
  `5fffb63d9f7e5ebbdd2261b13aec0e33ee7eae9ef0cc8b3b324edfc3674a6c69`;
- repeated full analysis produced byte-identical 17 fact shards;
- repeated coverage JSON was byte-identical;
- repeated content fingerprint was identical;
- validator processed 27,600 facts successfully;
- full build plus validation elapsed: 14.53 seconds;
- peak RSS: approximately 660 MiB.

## Known limitations

- Runner/KLC ingestion is not part of this core-only iteration.
- Full diagnostic outputs are still generated by core but are outside the canonical manifest.
- Uncompressed JSONL is used for the first ingestion contract.
- Static-analysis coverage remains partial where evidence is ambiguous or missing.

## Package checks

- Source-tree manifest: passed.
- Clean ZIP extraction and internal-manifest validation: passed.
- Clean ZIP `compileall`: passed.
- Clean ZIP focused and adjacent regression: 119 passed, 0 failed.

## Not run

The full multi-module platform regression was not run for this bounded core-only iteration.
