# code-analyzer-core 0.41.0

Adds the lightweight `repository-interaction-lineage.yaml` profile. The profile
reuses the canonical repository foundation and materializes only direct Java
`data_flow` and `field_flow` evidence needed for intersystem HTTP attribute
lineage. Persistence, conceptual data model, declared-value expansion and
execution-context traceability are excluded.
