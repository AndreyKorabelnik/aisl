# knowledge-layer-core 0.17.1

- Finalized ownership of workspace data-model schema, materialization, query, evidence, validation, selection and model-relation logic.
- Moved the complete implementation test suite from WKL to KLC.
- Added workspace build/query/validation entry points to the public `__all__` contract.
