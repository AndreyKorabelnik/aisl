# knowledge-layer-core 0.28.0

## Iteration 23

- materializes collection storage-key lineage as first-class canonical relationship evidence;
- attaches lineage to declared and observed polymorphic concrete targets by exact source/field/target matching;
- preserves every observed target alias and the shared storage-key field;
- exposes `storage_collection_requires_encoding` without inventing membership/container SQL;
- advances the workspace schema to `workspace_data_model/v16`.
