# Test status — knowledge-layer-core 0.59.35

- targeted attribute-extension query + affected materialization/contracts/runtime tests: **23 passed**;
- compileall: PASS;
- import/version: PASS (`0.59.35`);
- no Core changes;
- read query performs filtering/JSON decoding/related-anchor+gap projection only; no new inference or SQL generation.
