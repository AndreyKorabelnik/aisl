# knowledge-layer-core 0.53.7

Portfolio topology now consumes the dedicated canonical suite task `portfolio-topology` instead of the product-reporting `system-description` task. The topology schema, matching rules, islands and JSON contracts are unchanged.
