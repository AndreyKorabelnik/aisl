# knowledge-integration 0.1.9

- Attribute-addition profile v11 consumes storage-reference field observations without treating them as confirmed SQL columns.
- Consumers must use `relationship_relevance` on SQL JOIN examples: exact examples have priority; related examples remain useful analog evidence but cannot be presented as an existing exact JOIN.
- The profile explicitly permits a strongly supported source-extraction proposal when storage/encoding evidence is observed but current SQL usage is missing, with the gap kept visible.
- Bumped canonical tool catalog version to 3 because the attribute-extension tool guidance changed.
