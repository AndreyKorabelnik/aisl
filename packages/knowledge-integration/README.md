# knowledge-integration

Neutral public consumer contract for capability-gated access to a published Knowledge API revision.

Owns the typed `llm_integration_profile/v1`, canonical tool definitions/API bindings, capability gating, reusable grounding policy, retrieval profiles and deterministic Consumer Kit renderers.

It does not contain an LLM client, dialogue loop, provider configuration, source analysis or knowledge production.
