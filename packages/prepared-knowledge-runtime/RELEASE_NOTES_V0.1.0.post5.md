# prepared-knowledge-runtime 0.1.0.post5

- Ranked deterministic lexical retrieval for code-declared objects.
- Bounded observed `match_evidence` exposes the type/field fact that caused a hit.
- `binding_summary` exposes observed incoming/outgoing declared relationships without inferring business meaning.
- Search scope is explicit (`type_annotation_projection` or `all_declared_types`).
- Query contract version is `code-declared-data-model-query/v2`; prepared artifact schema remains `code-declared-data-model/v1`.
