# Prepared Knowledge Runtime 0.1.0.post1

- Attribute-extension read filters now accept either stable type/field occurrence IDs or FQCN/field names.
- This fixes the standard declared-model -> attribute-extension consumer hand-off without adding inference.
- No materialization, producer, schema, or SQL-generation behavior changed.
