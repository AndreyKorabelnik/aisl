# Analysis UI 2.0.0a43

- Adds the workspace-only “Зависимости между системами” master.
- Uses the fixed `system-interaction-lineage` runner suite.
- Uses `workspace-interaction/v1` reporting.
- Requires interaction-lineage and repository-coverage Knowledge Layer capabilities.
- Keeps zero, ambiguous and unresolved interaction results explicit instead of inventing links.
- Adds a specialized revision-chat welcome for interaction analysis.
