# Knowledge Control Plane 1.2.0a5

Adds the S2T reconstruction scenario for exactly two source classes: one datamart repository and one supplied PDM. The profile composes existing `sql-source-inventory` and `physical-data-model`; SQL lineage continues to include Runner-owned internal `sql-target-source-mapping`. No UCP repository or code-declared logical model is required.
