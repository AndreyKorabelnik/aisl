# Analysis UI 2.0.0a45

- Adds the workspace master “Модель данных”.
- Uses fixed runner suite `data-model`; no profile-name fallback is allowed.
- Requires `suite.data-model` and `common.data-model`.
- Uses fixed report `data-model-report/v1`.
- Adds a data-model chat welcome and example questions.
