# Analysis UI 2.0.0a39

- Revision-first home page with profile masters, recent revisions and an Advanced placeholder.
- Dedicated route for a selected analysis profile; repository/workspace forms receive the profile from the master.
- Revision detail exposes report and standard chat actions.
- A single revision can be pinned into a reusable standard assistant context without reanalysis.
- Empty chats show a deterministic capability welcome and clickable example questions without adding a synthetic message to model history.
- Cross-system attribute-addition chat remains unchanged; single-revision chat uses a separate generic instruction profile.
