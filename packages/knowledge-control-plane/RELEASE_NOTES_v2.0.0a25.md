# Analysis UI 2.0.0a25

Adds a prepared assistant context for the standard chat. A context pins independent
Knowledge API revisions for the source model, SQL datamart and optional PDM. It stores
only orchestration references and conversation metadata; DuckDB facts remain owned by
Knowledge API.

This release does not execute an LLM yet and does not change repositories or SQL.
