# Analysis UI 2.0.0a44

- Adds the workspace master “Описание системы”.
- Uses the fixed runner suite `default-system-analysis`; no analysis-profile fallback is allowed.
- Requires `suite.system-description` and `suite.data-model` capabilities.
- Uses the fixed report `system-description/v1`.
- Adds a system-description chat welcome and example questions.
