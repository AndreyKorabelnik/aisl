# Analysis UI 2.0.0a37

- Dependency floor raised to `knowledge-assistant>=0.14.4,<0.15.0`.
- Assistant profile integration test now validates the version exported by the installed public profile contract instead of a stale hard-coded content version.
- Runtime wizard and frontend behavior are unchanged.
