# Analysis UI 2.0.0a26

Connects the prepared assistant context to the real in-process Knowledge Assistant.
Every cross-system tool call is pinned to the selected SQL-datamart and source-model
revisions. Answers are returned as standard Markdown and persisted in the existing
conversation model. Repository files are never changed.
