# Changed files — Analysis UI 2.0.0a56

- `pyproject.toml` — requires `knowledge-assistant>=0.14.8,<0.15.0`.
- version metadata and OpenAPI — bumped to 2.0.0a56.
- `tests/test_assistant_profile_integration.py` — verifies Mermaid policy reaches the standard revision chat.
- `tests/test_assistant_target_handoff.py` — verifies the new assistant dependency floor.
