# Static Analysis Runner 0.10.22

Adds a generic bounded optional-evidence policy and repository metadata propagation used by Repository Inventory. `existing_only` optional evidence never starts a missing Core producer, while already available typed evidence remains reusable. Repository metadata is orchestration metadata, not a replacement for Core observed evidence, and affects only the Repository Inventory materialization identity where relevant.
