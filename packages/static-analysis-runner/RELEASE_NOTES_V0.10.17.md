# static-analysis-runner 0.10.17

Removes the unused system-wide `mechanism-catalog` diagnostic and its reverse dependency on source trees owned by other modules. Production execution behavior is unchanged. Historical validation/release artifacts remain provenance only.
