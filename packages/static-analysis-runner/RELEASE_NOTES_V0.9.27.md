# static-analysis-runner 0.9.27

- Added the `physical-model` command for deterministic PowerDesigner PDM analysis.
- Added `--physical-model-manifest` to Knowledge Layer materialization.
- PDM materialization uses the public `build_physical_model_knowledge_layer` contract from Knowledge Layer Core.
- Existing repository, workspace and suite behavior is unchanged.
