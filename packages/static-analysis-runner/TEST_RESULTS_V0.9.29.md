# Test results 0.9.29

- Built-in suite catalog, repository-suite and workspace-suite focused tests: 12 passed, 16 deselected.
- `data-model` resolves to the packaged canonical YAML and contains only the `data-model` task.
- Python compileall: passed.
- Setuptools package-data includes `config/suites/*.yaml`.
- Source manifest and ZIP integrity: checked after packaging.
