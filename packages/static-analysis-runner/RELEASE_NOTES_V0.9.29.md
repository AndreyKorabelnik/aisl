# Static Analysis Runner 0.9.29

- Adds the built-in `data-model` suite with the single canonical task `data-model`.
- Allows UI clients to address data-model analysis by exact suite ID without profile-name fallback.
